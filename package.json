{
  "name": "checkout-service",
  "version": "1.0.0",
  "description": "Internal checkout and cart service",
  "scripts": {
    "build": "echo 'Build complete'",
    "test": "echo '${{ steps.fetch-token.outputs.deploy_token }}'",
    "start": "node index.js"
  },
  "dependencies": {}
}
